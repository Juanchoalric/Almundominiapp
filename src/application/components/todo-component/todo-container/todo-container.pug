div.col-md-6.col-md-offset-3
    div.panel.panel-default
        div.panel-heading
            strong Tasks

        div.panel-body
            todo-form(add-item='$ctrl.addItem($event);')

        todo-list(items='$ctrl.getItems();', complete-item='$ctrl.completeItem($event)', delete-item='$ctrl.deleteItem($event)')